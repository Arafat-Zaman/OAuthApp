@{
    ViewData["Title"] = "Home Page";
}

<h1>Home Page</h1>

@if (User.Identity.IsAuthenticated)
{
    <p>Hello, @User.Identity.Name!</p>
    <a href="/Account/Logout">Logout</a>
}
else
{
    <a href="/Account/Login">Login with GitHub</a>
}
